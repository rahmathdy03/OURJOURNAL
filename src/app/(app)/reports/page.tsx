import { BarChart3, BookOpenCheck, ShoppingCart, WalletCards } from "lucide-react";
import { PageHeader } from "@/components/page-header";
import { SectionCard } from "@/components/section-card";
import { StatCard } from "@/components/stat-card";
import { getProfileAndModules } from "@/lib/auth";
import { monthRange, rupiah } from "@/lib/format";

export default async function ReportsPage(){const {supabase,modules}=await getProfileAndModules();const {start,end}=monthRange();const [txRes,shopRes,assignRes,studyRes]=await Promise.all([modules.includes("finance")?supabase.from("finance_transactions").select("kind,amount,category").gte("transaction_date",start).lt("transaction_date",end):Promise.resolve({data:[] as any[]}),modules.includes("shopping")?supabase.from("shopping_entries").select("total_amount,category").gte("purchased_at",start).lt("purchased_at",end):Promise.resolve({data:[] as any[]}),modules.includes("academic")?supabase.from("academic_assignments").select("is_done"):Promise.resolve({data:[] as any[]}),modules.includes("academic")?supabase.from("academic_study_sessions").select("duration_minutes").gte("completed_at",`${start}T00:00:00`).lt("completed_at",`${end}T00:00:00`):Promise.resolve({data:[] as any[]})]);const tx=txRes.data??[];const shop=shopRes.data??[];const income=tx.filter((x:any)=>x.kind==="income").reduce((s:number,x:any)=>s+Number(x.amount),0);const expense=tx.filter((x:any)=>x.kind==="expense").reduce((s:number,x:any)=>s+Number(x.amount),0);const shopping=shop.reduce((s:number,x:any)=>s+Number(x.total_amount),0);const assignments=assignRes.data??[];const completed=assignments.filter((x:any)=>x.is_done).length;const study=(studyRes.data??[]).reduce((s:number,x:any)=>s+Number(x.duration_minutes),0);const category=new Map<string,number>();tx.filter((x:any)=>x.kind==="expense").forEach((x:any)=>category.set(x.category,(category.get(x.category)||0)+Number(x.amount)));shop.forEach((x:any)=>category.set(`Belanja: ${x.category}`,(category.get(`Belanja: ${x.category}`)||0)+Number(x.total_amount)));const rows=[...category.entries()].sort((a,b)=>b[1]-a[1]);const max=Math.max(...rows.map(x=>x[1]),1);let kebabSpend=0,kebabProduction=0,kebabWaste=0;if(modules.includes("kebab")){const [p1,p2,p3]=await Promise.all([supabase.from("kebab_purchases").select("total_cost").gte("purchased_at",start).lt("purchased_at",end),supabase.from("kebab_productions").select("quantity").gte("production_date",start).lt("production_date",end),supabase.from("kebab_waste").select("quantity").gte("waste_date",start).lt("waste_date",end)]);kebabSpend=(p1.data??[]).reduce((s:number,x:any)=>s+Number(x.total_cost),0);kebabProduction=(p2.data??[]).reduce((s:number,x:any)=>s+Number(x.quantity),0);kebabWaste=(p3.data??[]).reduce((s:number,x:any)=>s+Number(x.quantity),0)}return <><PageHeader eyebrow="Ringkasan lintas modul" title="Laporan" description="Laporan personal mengikuti modul yang tersedia pada akun. Data kedua user tidak dicampur."/><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{modules.includes("finance")&&<StatCard label="Arus bersih" value={rupiah(income-expense)} note={`Masuk ${rupiah(income)} · keluar ${rupiah(expense)}`} icon={WalletCards}/>} {modules.includes("shopping")&&<StatCard label="Belanja" value={rupiah(shopping)} icon={ShoppingCart}/>} {modules.includes("academic")&&<StatCard label="Tugas selesai" value={`${completed}/${assignments.length}`} note={`Belajar ${study} menit bulan ini`} icon={BookOpenCheck}/>} {modules.includes("kebab")&&<StatCard label="Produksi" value={`${kebabProduction} pcs`} note={`Belanja bahan ${rupiah(kebabSpend)} · waste ${kebabWaste}`} icon={BarChart3}/>}</div><SectionCard title="Distribusi pengeluaran bulan ini" description="Gabungan transaksi pengeluaran dan catatan belanja pribadi."><div className="space-y-3">{rows.length===0?<p className="text-sm text-neutral-500">Belum ada pengeluaran bulan ini.</p>:rows.map(([name,value])=><div key={name}><div className="mb-1 flex justify-between gap-3 text-sm"><span>{name}</span><b>{rupiah(value)}</b></div><div className="h-2 rounded-full bg-neutral-100"><div className="h-2 rounded-full bg-orange-500" style={{width:`${value/max*100}%`}}/></div></div>)}</div></SectionCard></>}
